lets write here some simple function and modify it in 2 branches

test(int a, int b)
{

    if(a>b)
    {
    return a-b/b;
    }
// tu genialnie main
    if(b>a)
    {
    return b-a;
    }

return a*b/b;
}
