lets write here some simple function and modify it in 2 branches

test(int a, int b)
{
adding new stuff here

    if(a>b)
    {
    return a-b/b; // zostawiłem wersje z main
    }
// robak good
// tu genialnie main
    if(b>a)
    {
    return b-a;
    }
// dobrze robak
return a*b; //zostawiamy wersje robaka
}
